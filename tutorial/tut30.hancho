# tutorial/tut30.hancho
from hancho import Config

build_config = build_config + Config(build_tag = "tut30")

rules = build_config.include("tut30_rules.hancho")

app = rules.c_binary(
  source_files = ["src/main.cpp", "src/util.cpp"],
  build_files  = "app",
)
