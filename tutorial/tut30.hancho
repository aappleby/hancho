# tutorial/tut30.hancho
import hancho

build_config.build_tag = "tut30"

rules = hancho.include(build_config, "tut30_rules.hancho")

app = rules.c_binary(
  build_config,
  source_files = ["src/main.cpp", "src/util.cpp"],
  build_files  = "app",
)
