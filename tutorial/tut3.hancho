# tutorial/tut3.hancho
from hancho import *

build_config.defaults(
  command_path = Path.cwd(),
  source_path  = Path.cwd() / "src",
  build_path   = Path.cwd() / "build/tut3",
)

rules = load("rules.hancho", build_config)

app = rules.c_binary(
  source_files = ["main.cpp", "util.cpp"],
  build_files  = "app",
)
