from hancho import *

this_path = Path(__file__).parent

rules = load(
  hancho_file  = "rules.hancho",
  command_path = this_path,
  source_path  = this_path / "src",
  build_path   = this_path / "build/tut3",
)

app = rules.c_binary(
  source_files = ["main.cpp", "util.cpp"],
  build_files  = "app",
)
