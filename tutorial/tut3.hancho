# tutorial/tut3.hancho
from hancho import *

build_config.build_tag = "tut3"

rules = include("rules.hancho", build_config)

app = rules.c_binary(
  source_files = ["src/main.cpp", "src/util.cpp"],
  build_files  = "app",
)
