import hancho

app = hancho.Task(
    desc = "Compile and link build/app",
    command = [
        "mkdir -p build",
        "g++ src/main.cpp src/util.cpp -o build/app"
    ]
)

def bloop(task):
    print(f"Hello task {task._name}")

hancho.Task(desc = "blarp", command = bloop)
