# tutorial/src/src.hancho
from hancho import *

build_config.source_path = build_config.mod_path

rules = build_config.include("{rules_path}/tut40_rules.hancho")

rules.c_binary(glob("*.cpp"), "app")
