# tutorial/tut00.hancho

#hancho.repo_name = "tut00"

blep = hancho.task(
  desc = "lkasjdf",
  command  = [
    "mkdir -p build",
    "g++ {in_src} -o {out_bin}",
  ],
  in_src = ["src/main.cpp", "src/util.cpp"],
  out_bin = "tut00_app",
)


print(hancho)
print(hancho.exports)