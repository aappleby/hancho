import hancho

hancho.Rule(
  desc      = "Always rebuild {files_out} if the rule has no inputs",
  command   = "touch {files_out}",
  files_in  = [],
  files_out = "build/always_rebuild_if_no_inputs.txt"
)()
