import hancho
import os

hancho.Rule(
  desc      = "Does create its output file",
  command   = "touch {files_out[0]}",
  files_in  = "src/does_not_exist.txt",
  files_out = "build/missing_src.txt"
)()
