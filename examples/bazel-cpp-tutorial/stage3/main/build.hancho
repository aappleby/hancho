import hancho

hello_greet = hancho.tools.cc_library(
    name    = "hello-greet",
    in_srcs = ["hello-greet.cc"],
    in_hdrs = ["hello-greet.h"],
)

hello_world = hancho.tools.cc_binary(
    name    = "hello-world",
    in_srcs = ["hello-world.cc"],
    in_deps = [hello_greet, hancho.lib.hello_time],
)
