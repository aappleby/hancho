# tests/recursive_base_is_bad.hancho

hancho.config.build_dir = __name__

rule1 = hancho.Rule(
  command = "{thing}",
  thing = ":"
)

rule2 = rule1.extend(
  thing = "{base.thing}"
)

rule3 = rule2.extend(
  thing = "{base.thing}"
)

rule3()
