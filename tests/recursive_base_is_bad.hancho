# tests/recursive_base_is_bad.hancho

config.build_dir = "build"

rule1 = Rule(
  command = "{thing}",
  thing = ":"
)

rule2 = rule1.extend(
  thing = "{base.thing}"
)

rule3 = rule2.extend(
  thing = "{base.thing}"
)

rule3(__file__, "result.txt")
