# Load both repos directly.
import hancho

print(build_config)

print(build_config.Config(foo = 1))

#hancho.Task(
#  command = "touch {rel_build_files}",
#  source_files = [],
#  build_files = "top.txt"
#)

#print(build_config)
#
#repo1 = hancho.repo("repo1", **build_config)
#
#print(repo1)

#hancho.load("repo1_test1.hancho", **repo1)

#repo2 = hancho.subrepo("repo1/repo2", **build_config)
#
#
#repo2.load("repo2_test1.hancho")
