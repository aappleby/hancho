"""Multiple rules creating same output file = bad"""
from hancho import *

build_config.defaults(
  command_path = Path.cwd(),
  source_path  = Path.cwd(),
  build_path   = Path.cwd() / "build",
)

rule = build_config.rule(command = "touch {rel_build_files}")

taskA = rule(__file__, "colliding_output.txt")
taskB = rule(__file__, "colliding_output.txt")
