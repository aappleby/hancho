from pathlib import Path

def sync_command(task):
  Path(task.action.build_files[0]).touch()
  return task.action.build_files

rule = build_config.rule(
  command = sync_command
)
rule([], "result.txt")
