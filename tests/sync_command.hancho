from pathlib import Path

def sync_command(task):
  Path(task.action.abs_build_files[0]).touch()
  return task.action.abs_build_files

rule = build_config.rule(
  command = sync_command
)
rule([], "result.txt")
