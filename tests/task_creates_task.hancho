import hancho

def callback(task):
  hancho.Task(
    command = "touch {rel_build_files}",
    source_files = [],
    build_files = "dummy.txt"
  )
  return []

hancho.Task(
  command = callback,
  source_files = [],
  build_files = []
)
