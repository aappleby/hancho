def callback(task):
  hancho.task(
    command = "touch {rel_build_files}",
    source_files = [],
    build_files = "dummy.txt"
  )
  return []

hancho.task(
  command = callback,
  source_files = [],
  build_files = []
)
