# Load both repos directly.

rule = build_config.rule(
  command = "touch {rel_build_files}",
)

rule([], "top.txt")

repo1 = build_config.subrepo("repo1")
repo2 = repo1.subrepo("repo2")

repo1.load("repo1_test1.hancho")
repo2.load("repo2_test1.hancho")
