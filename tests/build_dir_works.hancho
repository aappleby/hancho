# tests/build_dir_works.hancho

config.build_dir = "build/build_dir_works"

build_dir_works = Rule(
  desc = "Should put a file in build/build_dir_works",
  command = "touch {files_out}",
)

build_dir_works([], "result.txt")
