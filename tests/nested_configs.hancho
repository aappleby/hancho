from hancho import Config

a = Config(
  b = Config(
    bar = 1234
  ),
  c = Config(
    d = Config(
      e = Config(
        foo = "{b.bar}"
      ),
      blep = 123,
    )
  )
)

print(a.expand("{c.d.e.foo}"))

#print(a.c.d.e.expand(a.c.d.e.foo))
