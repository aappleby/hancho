from hancho import *

build_config.defaults(
  command_path = Path.cwd(),
  source_path  = Path.cwd(),
  build_path   = Path.cwd() / "build",
)

garbage_template = build_config.rule(command  = "{aklsjd*^@&#^$@)!()@$*)(flksjdlfkjldfk}")
garbage_template(__file__, "result.txt")
