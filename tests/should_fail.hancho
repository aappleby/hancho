# tests/should_fail.hancho
from hancho import *

build_config.defaults(
  command_path = Path.cwd(),
  source_path  = Path.cwd(),
  build_path   = Path.cwd()
)

should_pass = build_config.rule(
  desc = "This task should fail",
  command = "(exit 255)",
)

should_pass(__file__, [])
